import { StarredDirective } from './starred.directive';

describe('StarredDirective', () => {
  it('should create an instance', () => {
    const directive = new StarredDirective();
    expect(directive).toBeTruthy();
  });
});
